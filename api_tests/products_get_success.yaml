# ********RoostGPT********

# Test generated by RoostGPT for test amanRestAssured using AI Type  and AI Model 
# 
# Test generated for /products_get for http method type GET in artillery framework
# RoostTestHash=62232e7786
# 
# 

# ********RoostGPT********
config:
  target: "http://localhost:3000"
  phases:
    - duration: 60
      arrivalRate: 20
  environments:
    local:
      target: "http://localhost:3000"
  payload:
    path: "products_get_success.csv"
    fields:
      - "id"
      - "name"
      - "price"
    delimiter: "^|^"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Get All Products"
    flow:
      - log: "Starting Get All Products test..."
      - get:
          url: "/products"
          headers:
            ApiKeyAuth: "{{$processEnvironment.API_KEY_AUTH}}"
            BearerAuth: "{{$processEnvironment.BEARER_AUTH}}"
            OAuth2Auth: "{{$processEnvironment.OAUTH2_AUTH}}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "name"
            - hasProperty: "price"
      - think: 1
