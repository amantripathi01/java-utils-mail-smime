# ********RoostGPT********

# Test generated by RoostGPT for test amanRestAssured using AI Type  and AI Model 
# 
# Test generated for /products_get for http method type GET in artillery framework
# RoostTestHash=62232e7786
# 
# 

# ********RoostGPT********
config:
  phases:
    - duration: 60
      arrivalRate: 20
  environments:
    local:
      target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "products_get_success.csv"
    fields: ["id", "name", "price"]
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Get All Products"
    flow:
      - log: "Starting test scenario for 'Get All Products' API Endpoint"
      - get:
          url: "/products"
          headers:
            ApiKeyAuth: "{{$processEnvironment.AUTH_TOKEN}}"
            BearerAuth: "{{$processEnvironment.AUTH_TOKEN}}"
            OAuth2Auth: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - contentType: "application/json"
            - hasProperty: "id"
            - hasProperty: "name"
            - hasProperty: "price"
      - think: 1
