# ********RoostGPT********

# Test generated by RoostGPT for test amanRestAssured using AI Type  and AI Model 
# 
# Test generated for /products_get for http method type GET in artillery framework
# RoostTestHash=62232e7786
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "products_get_others.csv"
    fields:
      - "productId"
      - "productName"
    skipHeader: true
    delimiter: "^|^"
  headers:
    ApiKeyAuth: "{{$processEnvironment.AUTH_TOKEN}}"
    BearerAuth: "{{$processEnvironment.AUTH_TOKEN}}"
    OAuth2Auth: "{{$processEnvironment.AUTH_TOKEN}}"

scenarios:
  - name: "Testing non-successful API requests"
    flow:
      - log: "Sending GET request to /products with invalid product ID"
      - get:
          url: "/products/{{ productId }}"
          expect:
            - statusCode: 404
            - hasHeader: "Content-Type"
            - headerEquals: {"Content-Type": "application/json"}
      - think: 1

      - log: "Sending GET request to /products with missing authentication header"
      - get:
          url: "/products/{{ productId }}"
          headers:
            BearerAuth: ""
          expect:
            - statusCode: 401
            - hasHeader: "Content-Type"
            - headerEquals: {"Content-Type": "application/json"}
      - think: 1

      - log: "Sending GET request to an invalid URL"
      - get:
          url: "/invalid/{{ productId }}"
          expect:
            - statusCode: 404
            - hasHeader: "Content-Type"
            - headerEquals: {"Content-Type": "application/json"}
      - think: 1
