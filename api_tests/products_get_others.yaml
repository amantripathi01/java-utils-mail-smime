# ********RoostGPT********

# Test generated by RoostGPT for test amanRestAssured using AI Type  and AI Model 
# 
# Test generated for /products_get for http method type GET in artillery framework
# RoostTestHash=62232e7786
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "products_get_others.csv"
    fields:
      - "productId"
      - "productName"
    skipHeader: true
    delimiter: "^|^"
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Non-success API responses"
    flow:
      - log: "Invalid GET request with incorrect URL"
      - get:
          url: "/produc"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
      - expect:
          - statusCode: 404
      - think: 1

      - log: "GET request without authorization header"
      - get:
          url: "/products"
      - expect:
          - statusCode: 401
      - think: 1

      - log: "GET request with invalid authorization token"
      - get:
          url: "/products"
          headers:
            Authorization: "Bearer invalid_token"
      - expect:
          - statusCode: 403
      - think: 1

      - log: "GET request with valid authorization token but non-existing product"
      - get:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
      - expect:
          - statusCode: 404
      - think: 1
